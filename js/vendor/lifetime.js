// Lifetime is a js plugin which allows given user to estimate its life
// expectency according to various criteria.

// Instanciating current user TODO: Make this dynamic
var currentUser = {
  firstName : "John",
  lastName : "Doe",
  age : 25
};

// Returns estimated life expectency in seconds
// score: int ; the final test's result
function estimate(score) {
  var lifeExpectency = score; //TODO: Improve this equation
  return lifeExpectency;
};

function addToScore(score) {

};
